name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  ci-native:
    uses: ./.github/workflows/ci-native.yml
  docker-smoke:
    uses: ./.github/workflows/docker-smoke.yml
